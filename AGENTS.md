# SekAI 프로젝트 작업 규칙

이 문서는 이 저장소에서 작업하는 에이전트가 따라야 할 코드 작성, 브랜치, 커밋, Pull Request 및 병합 규칙을 정의한다.

## 기본 원칙

- `main` 브랜치는 항상 실행 및 배포 가능한 상태로 유지한다.
- `main` 브랜치에 직접 Push하지 않는다.
- 모든 변경은 별도의 작업 브랜치에서 진행하고 Pull Request를 통해 병합한다.
- 하나의 브랜치와 Pull Request에는 하나의 작업 목적만 포함한다.
- 환경 변수, 비밀키, 개인정보, 불필요한 로그 및 임시 코드를 커밋하지 않는다.

## Python 코드 기준: NumPy

- Python 코드의 공식 기준은 [NumPy 공식 GitHub 저장소](https://github.com/numpy/numpy)의 현재 Python 구현 스타일이다.
- 규칙이 불명확하면 NumPy의 유사한 공개 함수 구현을 확인하고 함수 구성, 이름, 입력 검증, 오류 처리 및 문서화 방식을 따른다.
- 함수와 변수는 의미가 분명한 `snake_case`, 상수는 `UPPER_CASE`로 작성한다.
- 입력 검증은 주요 처리보다 먼저 수행하고 실패 원인을 알 수 있는 구체적인 예외 메시지를 제공한다.
- 배열 형태로 다루는 수치 입력은 적절한 경우 `np.asarray`로 정규화한다.
- 반복문보다 배열 연산과 불리언 마스크가 더 명확하고 효율적이면 NumPy 방식의 벡터화를 우선한다.
- 벡터화가 메모리 사용량이나 가독성을 크게 해치면 단순하고 명확한 반복문을 사용한다.
- NumPy 내부의 호환성 디스패처, 비공개 데코레이터 및 레거시 처리 코드는 프로젝트에 실제로 필요한 경우에만 도입한다.

## 코드 가독성

- 코드는 책을 읽듯 위에서 아래로 자연스럽게 흐름을 이해할 수 있게 작성한다.
- 파일 상단에는 진입점, 주요 흐름 및 공개 인터페이스를 두고 세부 구현과 보조 함수는 그 아래에 배치한다.
- 함수와 클래스는 하나의 명확한 책임만 가지게 하고 이름만으로 의도를 알 수 있게 작성한다.
- 축약어, 모호한 이름, 매직 넘버 및 지나치게 영리한 한 줄 코드를 피한다.
- 같은 추상화 수준의 코드를 가까이 두고 서로 다른 책임을 한 함수 안에 섞지 않는다.
- 깊은 중첩을 피하고 필요한 경우 조기 반환과 의미 있는 보조 함수를 사용한다.
- 중복을 줄이되 한 번만 사용하는 짧은 로직까지 무조건 추상화하지 않는다.
- 새로운 기능을 추가할 때 기존 코드의 읽기 흐름과 일관성을 함께 개선한다.

### 주석

- 모든 Python 함수와 메서드에는 NumPy 스타일의 Numpydoc 문서 주석을 작성한다.
- 문서 주석의 첫 문장에는 함수가 수행하는 동작을 짧고 명확하게 요약한다.
- 매개변수가 있으면 `Parameters` 절에 이름, 타입, 역할, 허용되는 값 또는 중요한 제약을 작성한다.
- 반환값이 있으면 `Returns` 절에 이름, 타입 및 의미를 작성하고 반환값이 없으면 해당 절을 생략한다.
- 호출자가 알아야 하는 예외가 있으면 `Raises` 절에 예외 타입과 발생 조건을 작성한다.
- 추가 설명이 실제 이해를 돕는 경우에만 `Notes`, `Examples` 또는 `See Also` 절을 사용한다.
- Python 이외의 언어에서는 Numpydoc과 동일한 정보를 담는 해당 언어의 표준 문서 주석 형식을 사용한다.
- 함수 내부의 자명한 코드를 줄마다 설명하는 주석은 작성하지 않는다.
- 함수 내부 주석은 비직관적인 알고리즘, 호환성 처리 또는 중요한 제약을 설명할 때만 간결하게 작성한다.
- 오래되어 코드와 맞지 않는 주석은 즉시 수정하거나 삭제한다.
- 주석 처리된 코드, 장식용 주석 및 의미 없는 구분선은 남기지 않는다.

## 파일 및 디렉터리 구성

- 하나의 파일은 하나의 주요 책임을 중심으로 구성한다.
- 새 파일을 만들기 전에 기존 파일이나 모듈에 자연스럽게 포함할 수 있는지 먼저 확인한다.
- 몇 줄의 일회성 코드나 한 곳에서만 사용하는 타입을 분리하기 위해 불필요한 파일을 만들지 않는다.
- 재사용성, 독립적인 테스트 가능성 또는 책임 분리가 분명하게 좋아질 때만 새 파일로 분리한다.
- 한 파일에 서로 독립적인 책임이 섞여 전체 흐름을 이해하기 어려워지면 책임별로 분리한다.
- 파일과 디렉터리 이름만 보고 역할과 위치를 예상할 수 있게 구체적으로 이름을 짓는다.
- 관련 파일은 기능 또는 도메인 단위로 가까이 배치하고 `misc`, `common`, `utils` 같은 포괄적인 공간에 무분별하게 모으지 않는다.
- 파일 수를 줄이거나 늘리는 것 자체를 목표로 삼지 않고 응집도와 탐색 용이성을 기준으로 결정한다.
- 사용하지 않는 파일, 빈 파일 및 중복 파일은 남기지 않는다.

## 브랜치

- 작업 브랜치는 최신 `main` 브랜치에서 생성한다.
- `develop` 브랜치는 사용하지 않는다.
- 브랜치 이름은 `<type>/<description>` 형식을 사용한다.
- `description`은 영문 소문자와 숫자를 사용하고 단어는 하이픈(`-`)으로 구분한다.
- 개인 이름, `update`, `final`, `real-final`처럼 목적을 알 수 없는 이름은 사용하지 않는다.

브랜치 타입은 다음 기준으로 선택한다.

| 타입 | 용도 |
| --- | --- |
| `feature` | 새로운 기능 개발 |
| `fix` | 버그 수정 |
| `refactor` | 기능 변화 없는 코드 구조 개선 |
| `docs` | 문서 작성 및 수정 |
| `design` | UI 및 스타일 변경 |
| `test` | 테스트 작성 및 수정 |
| `chore` | 설정, 패키지 및 개발 환경 작업 |
| `perf` | 성능 개선 |
| `hotfix` | 운영 환경의 긴급 오류 수정 |

예시:

```text
feature/document-search
fix/login-error-message
docs/update-readme
```

## 커밋

- 커밋 메시지는 `<type>: <description>` 형식으로 작성한다.
- 커밋 제목 앞에 프로젝트명, 이슈 키, 대괄호 또는 다른 식별자를 붙이지 않는다.
- 하나의 커밋에는 하나의 의미 있는 변경만 포함한다.
- 서로 관련 없는 변경은 별도의 커밋으로 분리한다.
- 기능 구현과 대규모 포맷 변경을 같은 커밋에 포함하지 않는다.
- 변경 대상을 구체적으로 표현하고 문장 끝에 마침표를 사용하지 않는다.
- `수정`, `업데이트`, `작업 완료`처럼 변경 내용을 알 수 없는 설명은 사용하지 않는다.
- 가능한 한 제목을 50자 이내로 작성한다.

커밋 타입은 다음 기준으로 선택한다.

| 타입 | 용도 |
| --- | --- |
| `feat` | 새로운 기능 추가 |
| `fix` | 버그 수정 |
| `refactor` | 기능 변화 없는 코드 구조 개선 |
| `docs` | 문서 작성 및 수정 |
| `design` | UI, 스타일 및 디자인 시스템 변경 |
| `style` | 코드 포맷, 공백 및 세미콜론 변경 |
| `test` | 테스트 코드 작성 및 수정 |
| `chore` | 설정, 패키지 및 개발 환경 작업 |
| `perf` | 성능 개선 |
| `ci` | CI/CD 설정 변경 |
| `build` | 빌드 시스템 및 의존성 변경 |
| `revert` | 이전 변경 되돌리기 |

예시:

```text
feat: 문서 검색 기능 추가
fix: 로그인 오류 메시지 표시 수정
docs: 로컬 실행 방법 추가
```

## Pull Request

- Pull Request의 대상 브랜치는 `main`으로 한다.
- Pull Request 제목은 커밋과 동일하게 `<type>: <description>` 형식으로 작성한다.
- 제목 앞에 프로젝트명, 이슈 키, 대괄호 또는 다른 식별자를 붙이지 않는다.
- 변경 범위와 무관한 파일을 포함하지 않는다.
- Pull Request 본문에는 반드시 `달라진 점`과 `변경 근거`를 작성한다.
- 구현 방식이나 설계에 선택이 있었다면 선택한 방식, 선택 근거, 고려한 대안 및 트레이드오프를 작성한다.
- 별도의 선택이 없었다면 `선택과 트레이드오프` 항목에 `해당 없음`이라고 작성한다.

Pull Request 본문은 다음 형식을 사용한다.

```markdown
## 달라진 점

-

## 변경 근거

- 이 변경이 필요한 이유:
- 참고한 요구사항 또는 문제:

## 선택과 트레이드오프

- 선택한 방식:
- 선택 근거:
- 고려한 대안:
- 장점:
- 단점 및 제약:
```

## 병합

- 기본 병합 방식은 일반 Merge commit(`Create a merge commit`)을 사용한다.
- Squash and Merge를 사용하지 않는다.
- 병합을 위해 기존 커밋을 합치거나, 재정렬하거나, 수정하지 않는다.
- 병합 전에 Pull Request 본문의 `달라진 점`, `변경 근거`, `선택과 트레이드오프`를 최종 상태로 정리한다.
- 필수 리뷰와 CI가 완료되고 충돌이 해결된 경우에만 병합한다.
- 병합이 완료되면 작업 브랜치를 삭제한다.
